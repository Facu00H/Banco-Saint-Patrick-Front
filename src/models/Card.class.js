export class Card {
    cardNumber;
    pin;
    balance;

    constructor(cardNumber, pin, balance){
        this.cardNumber = cardNumber;
        this.pin = pin;
        this.balance = balance;
    }
}